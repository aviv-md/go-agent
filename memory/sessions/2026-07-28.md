# 2026-07-28

## Milestone: Pair-programming contract

**Decided**

- Build an agent in **Go only**.
- AI pair role: align, remind, coach — **no writing code**, **no handing finished solutions**.
- At milestones / important decisions: log under `memory/sessions/` and update `memory/INDEX.md`.
- Contract encoded in `.cursor/rules/pair-programming.mdc` (`alwaysApply: true`).

**Open questions**

- Agent architecture and scope (not yet defined this session).

**Next focus**

- Align on what "the agent" should do and the first thin vertical slice.

---

## Milestone: Package separation

**Decided**

- Agent ≈ model + harness (loop, tools, skills, context, stop rules).
- Skills = Agent Skills with `SKILL.md`, not synonyms for tools.
- Packages: `internal/ai`, `internal/agent`, `internal/skill`, `internal/tools`.
- Deps: `agent` → `ai`/`skill`/`tools`; `skill` ↛ `tools`; `ai` stays leaf.
- Written up in `docs/architecture.md`.

**Open**

- Where tool schemas live (`tools` vs assembled in `agent`).
- Skill selection: model picks vs harness picks (discussed, not locked).

**Next focus**

- First thin vertical slice across the four packages.
