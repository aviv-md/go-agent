# 2026-07-29

## Milestone: First LLM call

**Decided / done**

- Prototype entrypoint: `cmd/main/main.go`.
- Provider: OpenRouter (`AI_BASE_URL` / `AI_API_KEY`) via openai-go v3 **Responses** API.
- Config: `internal/infra/config` — package-level `Env`, `godotenv` (warn if missing `.env`), `Load()` returns `Env` (idempotent).
- Verified: `go run ./cmd/main/main.go` → assistant reply (“Yep, I’m here…”).

**Open**

- Move client/call behind `internal/ai` (still inline in `main`).
- Chat Completions vs Responses long-term; schemas/tools later.
- Skill selection model vs harness (earlier, still open).

**Next focus**

- Fold the working call into `internal/ai`, keep `main` thin — or jump to agent loop / first tool.
