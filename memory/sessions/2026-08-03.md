# 2026-08-03

## Milestone: Task 8 done — one real tool + registry

**Decided / implemented** (all application code by the human; assistant coached)

- Added provider-neutral `Tool` metadata: name, description, JSON Schema input description, and a Go handler.
- Added `Registry` registration, listing, execution by name, and explicit unknown-tool errors.
- Added `NewRoomTemperatureTool`, a deterministic demo tool with a required string `room` argument.
- Kept runtime argument validation in the handler; deliberately did not build a JSON-Schema validator.
- Removed an unnecessary single-implementation interface and avoided OpenAI SDK types in `internal/tools`.
- Full `go test ./...` passes; `internal/tools` is gofmt-clean.

**Next focus**

- Task 9: give the model the registry's tool definitions, execute returned tool calls through the registry, append call-ID-bound observations, and prompt again.

## Milestone: Task 9 boundary decision

**Decided**

- Use `parameters` consistently for tool input metadata; the parameter object is represented as JSON Schema.
- Tool metadata stays beside the executable handler in `internal/tools`.
- Available tools cross the model boundary on each prompt, leaving room for per-turn selection without implementing a selector yet.

**Next focus**

- Finish the available-tool model boundary, then add the red agent test for one complete tool turn.

## Milestone: Task 9 live tool smoke passed

**Verified**

- A live `go run ./cmd/main/main.go` call advertised the room-temperature tool through the OpenAI-compatible provider.
- The model requested the tool, the registry returned the deterministic observation, and the model produced a final answer reporting 72°F.
- The full Go test suite passes, but the fake-model tests do not yet assert the call-ID-bound observation or advertised tool metadata.

**Next focus**

- Add the focused happy-path agent regression test before marking Task 9 done.

## Milestone: Task 9 accepted for demo scope

**Decided**

- Mark Task 9 done based on the passing full suite plus successful live end-to-end tool turn.
- Defer the focused transcript/metadata regression test as post-demo polish; this is an explicit speed-versus-coverage tradeoff.

**Next focus**

- Task 10: add the smallest useful ReAct system nudge.

## Milestone: Task 9 concerns deferred

**Decided**

- Move all remaining assistant-raised Task 9 coverage, policy, ownership, and cleanup concerns into `TODO.md` under `Post-demo Task 9 polish`.
- None of those items block the demo path.

**Next focus**

- Task 10 only; keep the remaining build minimal.

## Milestone: Task 10 done — visible ReAct lifecycle

**Implemented / verified**

- The agent receives a lightweight system nudge for brief visible status, one tool call per turn, observation use, and plain final responses.
- The CLI renders colored `Thought`, `Action`, `Observation`, and `Respond` lifecycle lines.
- Tool actions log before execution; observations show tool output rather than the internal message struct.
- Full `go test ./...` passes.

**Next focus**

- Task 11: smoke one tool prompt and one no-tool prompt, then freeze.

## Milestone: Task 10 polish explicitly deferred

**Decided**

- Keep the working inline transcript logs for the demo despite their rough structure.
- Defer a proper transcript sink/logger boundary, centralized styling, stable argument rendering, prompt ownership, and rendering tests to the post-demo backlog.

**Next focus**

- Do not refactor logging tonight; proceed to Task 11 smoke checks.

## Milestone: Tasks 11 and 12 done — demo path repeated

**Verified**

- No-tool prompt produced a single `Respond` with no Thought, Action, or Observation.
- The exact bedroom-temperature prompt produced the full Thought → Action → Observation → Respond lifecycle.
- The same happy path passed twice consecutively without code changes, returning the deterministic 72°F result.

**Next focus**

- Task 13 failure/build notes and Task 14 scope freeze; presentation synthesis and rehearsal remain.

## Milestone: Tasks 13–15 done — workshop deck drafted

**Produced**

- Added `docs/presentation.md`: a 15-slide, 45-minute predictive workshop for a general technical audience.
- The deck teaches model + harness, tools, the stop rule, and ReAct before the live demo.
- Added audience prediction cues, exact proven tool/no-tool prompts, a verified fallback transcript, four selective Go excerpts, build scars, deliberate exclusions, speaker notes, timing checkpoints, and overrun cuts.
- Completed a 45-minute tabletop timing pass; no application code changed.

**Next focus**

- Task 16 remains human-owned: deliver one cold rehearsal with the live prompt and narration.

## Milestone: Speaker materials packaged

**Produced**

- Added combined slide-by-slide speaker notes and rescue cues in English and Hebrew.
- Added standalone browser/print HTML versions, including RTL handling for Hebrew and LTR terminal/code blocks.
- Added `docs/README.md` to index presentation artifacts and identify Markdown as the editable source of truth.

**Next focus**

- Rehearse from the Hebrew or English cheatsheet, then mark Task 16 done.
